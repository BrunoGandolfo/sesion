# Versión nueva — 15 de septiembre de 2026

Base: origin/main e247d8b. Rama: version-nueva.

- El dashboard consulta GET /api/version al montar y al volver a primer plano (visibilidad, foco o pageshow). Si cambia el identificador de su JavaScript, muestra «Hay una versión nueva. Tocá para actualizar». Nunca recarga solo. En /grabar y sus subrutas no consulta ni muestra el botón; al salir retoma el chequeo. El toque comprueba también la ruta real del navegador.
- El identificador público se genera en next.config.ts y queda congelado en ambos bundles. No requiere configurar un secreto ni una variable manual. Endpoint público sin datos de cuenta ni consultas a la base, Cache-Control no-store y maxDuration 15. Los errores y pedidos colgados no interrumpen el trabajo. Sin service worker ni dependencias nuevas.
- Chromium a 390 px contra dos builds de producción locales: A cc3440ca-a483-48ef-aaa9-594ffdd8b11e, B 1c0dc6e9-e1b9-4930-bd53-e1e2c5314ebe. Se conservó la pestaña A abierta durante la compilación y el arranque de B: aviso visible, cero navegaciones automáticas, recarga sólo al tocar y aviso ausente después. Reiniciar B devolvió el mismo identificador. Login real con seed y Postgres exclusivo pg-sesion-version-nueva (60525); sin proveedores externos ni cambios de pacientes.
- Trece casos nuevos protegen versión igual/distinta, actualización manual, rutas de grabación, carrera de navegación, pérdida de red, respuesta inválida, timeout y contrato del endpoint. Se actualizó la lista explícita de rutas públicas del test de cookie, conservando coincidencia exacta. tsc sin errores, lint sin errores (un aviso previo), 1315 unitarios y 316 de integración en Docker en verde; el guardián de maxDuration pasó para las 52 rutas.
- No se modificaron grabador, Recorrido, schema, worker ni deploys. La PWA en un teléfono físico queda para el dueño. El aviso no guarda formularios pendientes: actualizar recarga la página por decisión explícita de quien toca. codex review --uncommitted se ejecutó sobre copia temporal del mismo commit y diff, por el archivo .codex del worktree raíz; sin tocar esa configuración ajena. Revisión final sin hallazgos accionables; inspeccionó los tests sin ejecutarlos en su entorno de solo lectura.
