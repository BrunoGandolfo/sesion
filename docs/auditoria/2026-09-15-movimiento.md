# Movimiento — 15 de septiembre de 2026

Base: origin/main e247d8b. Rama: movimiento.

- Tiempos 150/180/220 ms y curva única en src/lib/movimiento.ts, compartidos por CSS y Framer. Se quitaron conteos de importes, pulsos y giros continuos. Lupita queda quieta mientras espera o recibe texto; conserva gestos finitos al abrir, terminar y tocar el menú. Movimiento reducido deja estáticos tanto componentes como transiciones CSS.
- Guardado legible de 13 px junto al campo en ficha y configuración, sin confirmar un valor nuevo por la respuesta de uno anterior. Celdas del mes con cantidad de turnos y rótulo accesible. Encabezado de Lupita más breve, conservando el aviso de privacidad. No cambian contratos ni guardado en el servidor.
- Chromium a 390 px contra localhost:3134: cambios reales en nombre y nota privada, relectura de persistencia y restauración de los valores iniciales. CSS efectivo: 150 ms y cubic-bezier(0.16,1,0.3,1); con movimiento reducido: 0 s y transition-property none. Grilla: 42 celdas con cantidad accesible. Sin llamadas a proveedores. Se detectó y corrigió una diferencia de hidratación al recargar con movimiento reducido; la primera carga ahora usa la misma representación estática en servidor y navegador, con tests para ambas preferencias. La revisión detectó además un remontaje al hidratar; se conservan los mismos wrappers y los tests exigen identidad del nodo y un solo montaje de la pantalla y del panel plegable. La repetición de la prueba no produjo errores de hidratación. Queda el aviso previo de CSP Report-Only en consola.
- Tests de duración/curva, ausencia de indicadores animados, lectura inmediata de totales, estados del guardado y cantidades del mes. Postgres exclusivo pg-sesion-movimiento en 60524. tsc sin errores; lint sin errores (un aviso previo); 1318 unitarios y 316 de integración en verde. codex review --uncommitted sobre copia con idéntico commit base y diff: sin hallazgos accionables tras corregir el P2. Su entorno de solo lectura impidió ejecutar Vitest allí; las suites sí se ejecutaron en este worktree contra su Docker.
- Excepción de alcance informada antes de trabajar: no se editaron los archivos de grabador, Recorrido, schema ni worker. Los estilos locales de medidor-audio (100 ms) y barras de grabar-view y FeedbackTerapeutaView (200 ms/ancho) siguen pendientes de sus dueños. El criterio global de ninguna animación fuera de las tres duraciones no puede certificarse para esas áreas. No se ocultaron esas excepciones; el test de límites recorre el resto del código y las identifica expresamente.
