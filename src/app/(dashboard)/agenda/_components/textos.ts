// Textos propios de Agenda. Los que comparten otras pantallas viven en
// src/lib/glosario.ts.

/** Inicial de cada día de la tira de la semana, de lunes a domingo: la
 *  semana de la agenda empieza el lunes. */
export const INICIALES_DIAS = ["L", "M", "M", "J", "V", "S", "D"] as const;

/** aria-label del grupo de los siete días, en el teléfono. */
export const TIRA_SEMANA = "Días de la semana";
